//Pass in a reference to Core, just in case...
//Still need to figure out an elegant way to wire this up against the ViewModel...
define(["Boiler"], function (Boiler) {

    //var ValidationHandler = function(globalContext) {
    //    this.context = globalContext;
    //};

    ////Validation logic goes here...
    //ValidationHandler.prototype.validateSelectedStatus = function(vm) {
    //    if (vm.OperatingStatus.Id == 0) {
    //        alert('Please select a status2...');
    //        return false;
    //    }
    //    return true;
    //};

    //return ValidationHandler;
});